#include "main.h"
#include<stdio.h>
/**
 * _puts - Prints to stdout
 * @str: hold the string
 * Description: prints to stdout
 * Return: Always 0 on success
 */
void _puts(char *str);

/**
 * main - Entry point
 *
 * Description: The main function, entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
